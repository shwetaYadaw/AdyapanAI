/**
 * ADYAPAN — MongoDB → MySQL Migration Script
 *
 * ✅ MIGRATION COMPLETE — This script has already been run successfully.
 *    All data has been migrated from MongoDB to MySQL.
 *    MongoDB/Mongoose has been fully removed from the backend.
 *    This file is kept for historical reference only.
 *
 * To seed fresh data into MySQL, use: npx ts-node src/scripts/seed.ts
 */

console.log('✅ Migration already completed. MongoDB → MySQL migration is done.');
console.log('   Use src/scripts/seed.ts to seed fresh data.');
process.exit(0);
